$('#notice').hide();

if ($('#cart tr').length == 1) { 
	//$('#cart').show('blind', 1000); 
	$('#cart').show('blind', {direction: "horizontal"}, 1000);
}

$('#cart').html("<%= escape_javascript render(@cart) %>");

$('#current_item').css({'background-color':'#88ff88'}).animate({'background-color':'#114411'}, 1000);
// Затем мы вызвали метод css() чтобы установить начальный цвет фона, 
// а затем вызвали метод animate() для постепенного возвращения к исходному цвету, 
// используемому нашим макетом за период в 1000 мс, широко известный как одна секунда.